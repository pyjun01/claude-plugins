{
  "eval_id": 0,
  "eval_name": "greenfield-setup",
  "prompt": "/harness \"Build a simple hello world webpage\"",
  "assertions": [
    "Setup command includes --prompt with the user's prompt text",
    "A runId starting with 'run-' was extracted from harness.js JSON output",
    "The 'Setup complete. Run: ...' message was reported to user",
    "Empty directory correctly identified as GREENFIELD",
    "harness.js next was called with the correct --run-id flag",
    "PLAN action keyword was received and parsed from next output",
    "planner.md reference file was selected and read",
    "runId was appended to the agent prompt",
    "planner.md content (Runtime Context section) was injected into the agent prompt"
  ]
}
